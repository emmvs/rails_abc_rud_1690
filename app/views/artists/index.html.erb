<div class="container mt-2">
  <h1>Artists 🎨</h1>
  <p>Look at all these beautiful artists whuuuu</p>

  <!-- TODO: Iteration on Artists -->
  <% @artists.each do |artist| %>
    <div class="card p-3 m-4">
      <%= link_to artist.name, artist_path(artist.id), class: "btn" %>
    </div>
  <% end %>
</div>
